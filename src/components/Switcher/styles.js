import styled from 'styled-components';

export const Container = styled.div`
    color: ${({ theme }) => theme.colors.white} ;
    width: 4rem;
    height: 2rem;
    bottom: 5rem;
    left: 2rem;

    position: fixed;
`;
